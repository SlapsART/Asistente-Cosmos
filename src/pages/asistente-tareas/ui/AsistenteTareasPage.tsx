import { Box } from '@mui/material';
import { AsistenteTareasWidget } from '@/widgets/asistente-tareas';

export function AsistenteTareasPage() {
  return (
    <Box sx={{ height: '100%', overflow: 'hidden' }}>
      <AsistenteTareasWidget />
    </Box>
  );
}
