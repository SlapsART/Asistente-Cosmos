import { Box } from '@mui/material';
import { AsistenteBaseWidget } from '@/widgets/asistente-base';

export function AsistenteBasePage() {
  return (
    <Box sx={{ height: '100%', overflow: 'hidden' }}>
      <AsistenteBaseWidget />
    </Box>
  );
}
