import { Box } from '@mui/material';
import { AsistenteVerticalWidget } from '@/widgets/asistente-vertical';

export function AsistenteVerticalPage() {
  return (
    <Box sx={{ height: '100%', overflow: 'hidden' }}>
      <AsistenteVerticalWidget />
    </Box>
  );
}
